import styled from "styled-components";

const FormContainer = styled.div`
    background: red;
`;

const Contact = () => {
    return (
        <div>
            Contact
        </div>
    );
};

export default Contact;